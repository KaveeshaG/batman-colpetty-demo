apiVersion: v1
kind: Service
metadata:
  name: colpetty-guardian-service
  labels:
    app: colpetty-guardian
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: colpetty-guardian